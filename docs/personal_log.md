To do April 11 (goals):
1) polish the workout plan (no need to be so good, just workable)
2) connect nutrition API (need to design this). Basically the idea is, user can search ingredients of the food and see calories and macros / 100 gram
3) test to ensure that it is ready for production

To do April 2 (goals):
1) figure out how to cleanly combine meal builder (this will be related to the AI feature) + the add individual nutrition entry
2) work on the AI feature 
3) (finish or not finish but I must do this) update my portfoliio to include fitflow, also i think i should remove the animation seems tacky and probably most people can do this too with AI

Another issue: when searching for ingredient, there seems to be too many output. Proposal: average it out 

Proposal: instead of displaying exact calorie count, display a range

Another thing to do (user related convenience):
1) Remember me feature + authentication (database should only support 2 user without authentication, if there is more than 2 user, then database should authenticate it)
2) Revise the UI, to make it more interesting and usable (make the logging feature more usable)

TO DO (MUST DONE) April 17:
1) create a way to make the loading time (due to render) faster (ping every 15 minutes to avoid cold start on a cheap endpoint)
2) figure out a way to make the login faster (using login token) DOONE
3) develop the new page: recipe builder and create a directory system that displays all recipe
4) fix any bugs